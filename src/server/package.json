{
	"name": "lime-server",
	"private": true,
	"description": "Lime app's backend",
	"main": "src/server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"type": "module",
	"dependencies": {
		"dateformat": "^5.0.2",
		"dotenv": "^16.0.0",
		"express": "^4.17.2",
		"express-session": "^1.17.2",
		"mongoose": "^6.2.1",
		"passport": "^0.5.2",
		"passport-google-oauth2": "^0.2.0"
	}
}
